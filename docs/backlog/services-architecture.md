# Services and Architecture Backlog

## P0
- define shared managed-service control plane contract
- create capability registry spec
- define short-lived service auth
- classify browser, sandbox, and repo actions by safety level

## P1
- define managed-service rollout and fallback behavior
- document beta-only vs launch-ready service capabilities
- keep unsafe repo writes disabled by default

## P2
- productize browser as first-class managed service
- add safer repo credential brokerage and PR lifecycle support
